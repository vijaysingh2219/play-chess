apiVersion: apps/v1
kind: Deployment
metadata:
  name: play-chess-web-deployment
  namespace: play-chess
  labels:
    app: play-chess-web
spec:
  # replicas is intentionally omitted: the HorizontalPodAutoscaler
  # (k8s/web-hpa.yml) owns this value. Setting it here would reset the
  # pod count on every `kubectl apply`, fighting the autoscaler.
  selector:
    matchLabels:
      app: play-chess-web
  template:
    metadata:
      labels:
        app: play-chess-web
    spec:
      containers:
        - name: play-chess-web
          image: your-dockerhub-username/play-chess-web:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: play-chess-web-env
            - configMapRef:
                name: play-chess-config
          env:
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 30
            failureThreshold: 3
