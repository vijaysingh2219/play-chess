# Database connection string
# Important: If you run migration commands locally, replace `postgres`
# with the actual database host (for example: localhost or your DB host).
DATABASE_URL=postgresql://postgres:password@postgres:5432/play-chess-app
